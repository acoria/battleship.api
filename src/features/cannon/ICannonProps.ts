export interface ICannonProps {
  /**
   * Container size in rem
   */
  containerSize: number;
  turretColor?: string;
  barrelColor?: string;
  /**
   * Aim angle in degrees. 0 is straight up.
   */
  aimAngle?: number;
}
